# TruffleHog ignore file - exclude template files and false positives

# Configuration template files with placeholder values
config/.env.example
config/.env.template
config/mcp.json.example

# Example/template values - these are placeholders, not real secrets
your_gemini_api_key_here
your-google-api-key-here
your-api-key-here

# Documentation examples
docs/

# GitHub token references in CI (legitimate usage)
secrets.GITHUB_TOKEN
${{ secrets.GITHUB_TOKEN }}